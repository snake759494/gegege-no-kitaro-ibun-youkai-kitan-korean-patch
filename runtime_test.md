# 실행 확인 기록

## v1.0.3 — UI 재적용 (2026-09-16)

사용자가 재적용 후보에 대해 “제대로 진행되는걸 확인했습니다. 릴리즈 해주세요.”라고 확인했습니다. 배포 결과 ISO는 `Kitaro_KR_UI_candidate_20260916.iso`와 동일하며 SHA-256은 `b673e0832b9bd4e23ddf6527b836e8c90bab81e04b05502900bd4b7e6a9a9ca3`입니다. PCSX2 v2.9.52에서 냉부팅 → 병원 탐색 및 이동·회전도 확인했습니다. 프리징 근본 원인과 전체 실행 범위는 미확정입니다.

검증 규칙과 시험 결과는 [IMAGE_VALIDATION.md](IMAGE_VALIDATION.md)에 기록합니다. 아래 v1.0.1의 D_BG 원인 판정은 당시 사용자 진단 결과를 요약한 것으로, 입력·시작 상태를 통제한 인과 검증은 아니었습니다. 이미지 픽셀을 게임 로직이 사용한다는 설명 역시 확인되지 않은 가설입니다. 이번 작업에서는 그 가설을 확정 사실로 사용하지 않습니다.

## v1.0.2 (2026-09-16)

- 테스트 ISO: `Kitaro_DIAG_SYS.iso`, SHA-256 `77cc14cdeea44274e6b6b89e0b37dd0ae11504876561aff510f10d7200b4acf8`. 배포 패치를 적용한 결과도 같은 해시입니다.
- 실행 중인 PCSX2의 실제 버전은 v2.9.52입니다(설치 폴더명에 표시된 버전과 다름).
- 프롤로그 방에서 정지처럼 보이던 상태에서도 방향 입력에 따른 시점 회전과 이동이 가능함을 확인했습니다. ○ 자동 반복 입력을 해제하고 방향 입력을 확인한 뒤 사용자가 “지금 이후 진행이 됩니다”라고 확인했습니다.
- 진단 중 임시 상태 수정은 원래 상태 저장을 다시 불러와 되돌렸습니다. 최종 진행 상태의 실행 코드와 시나리오 버퍼가 초기 상태와 동일하고 한국어 C2800 대사가 유지됨을 비교했습니다. ISO 자체는 수정하지 않았습니다.
- 따라서 이번 릴리즈는 진행을 확인한 ISO 구성의 배포입니다. D_SYS 복원만의 효과, 자동 입력 해제와 방향 조작 각각의 효과는 분리 검증하지 않았습니다. 멈춤의 근본 원인 해결이나 전체 게임 완주를 의미하지 않습니다.
- 전체 ISO를 비교해 D_MOJI·D_MESS·D_CHFONT 외 모든 바이트가 원본과 동일함을 확인했습니다. D_SYS·D_BG·실행 파일은 원본입니다.

아래는 과거 버전의 당시 기록이며, 당시 원인 추정은 이번 구간의 확정 진단을 뜻하지 않습니다.

## v1.0.1 (2026-09-15, 결과 ISO SHA-1 `e9578650d14d45c8aa495aa6e051beec510e2810`)

- 제보: v1.0.0에서 프롤로그의 3D 탐색 화면(요괴 안테나 레이더가 보이는 방)에서 문을 열면 대사 없이 게임이 멈춤.
- 사용자가 PCSX2로 진단 빌드를 순서대로 확인: 대사 파일만 원본(A)은 미확인, UI 이미지 두 파일(D_SYS+D_BG) 원본(B) → 정상, D_BG만 원본(C) → 정상. 따라서 원인은 `D_BG_ko.BIN`.
- 변경 바이트는 전부 이미지 9장의 픽셀 영역 안이고 헤더·팔레트·이웃 팩은 무손상이었으므로, 게임이 이 이미지의 픽셀 값을 표시 외의 용도로 읽는 것으로 추정됩니다. 9장 중 어느 것인지는 더 좁히지 않고 사용자 결정으로 파일 전체를 원본으로 되돌렸습니다.
- v1.0.1 자체(D_BG 원본 + 나머지 한글)는 진단 빌드 C와 파일 구성이 같으며, C에서 해당 지점 통과를 확인했습니다. 그 이후 구간은 미확인입니다.

이전 기록의 에뮬레이터 확인은 사용자가 수행했으며, 빌드 스크립트와 검수 도구는 에뮬레이터를 실행하지 않습니다. 아래는 이 저장소를 공개하는 시점에 기록으로 남아 있는 범위입니다. 합성 미리보기(`--preview` 출력, `mess_render.py` 렌더링)는 게임 실행 화면으로 간주하지 않습니다.

## v1.0.0 빌드 (2026-09-10, 결과 ISO SHA-1 `396bbb0ccf92348477d07339ac36e67bcc7d2124`)

- 정독 검수 후 `D_MESS_ko.BIN`을 다시 주입하고 ISO·xdelta를 다시 만든 빌드입니다. 글꼴·이름표·UI 이미지·메뉴 배경 파일은 2026-09-07~09 빌드와 같습니다.
- 자동 검수(`qa_text.py`, `qa_img.py`)와 xdelta 왕복 검사는 이 빌드에서 통과했습니다.
- 이 빌드의 PCSX2 실행 확인은 사용자 몫으로 넘겼으며, 이 문서를 쓰는 시점에 확인 결과가 기록되어 있지 않습니다. 대사가 많이 바뀐 곳은 무지나 편(서브파일 0x78800), 큰지네 동굴(0x7c000), 기가 성 최종전(0x9a800), 오소레잔(0xb7800)입니다.

## 이전 빌드에서 PCSX2 2.2.0으로 확인한 것

- 2026-09-06: 한글 글꼴을 넣은 `D_MOJI.BIN`으로 부팅 → 타이틀 → 시작 → 인트로 내레이션의 한자 자리에 한글이 코드 계산값대로 표시됨. 프로브 빌드(모든 메시지 첫 두 글자를 코드 0x0B2D·0x0A00으로 교체)에서 빈 글리프 블록 12·13의 글자(힝·통)가 인트로 첫 줄에 정확히 표시되어 2,350자 전부 사용 가능함을 확인.
- 2026-09-07: 사용자의 플레이 스크린샷으로 초반 대사와 대사창 이름표를 확인. 이름표는 폰트가 아니라 `D_CHFONT.BIN`의 이미지라는 사실을 여기서 확정하고 한글 이름표를 만들었습니다. 이 단계에서 아직 번역되지 않은 메시지는 한자 자리에 무작위 한글이 섞여 보였는데, 이는 글꼴 오류가 아니라 미번역 메시지의 표시 특성입니다(현재는 전부 번역됨).
- 메인 메뉴의 글자(始めから 등)와 챕터 타이틀·상태 이상 등은 미리 렌더링된 이미지라 글꼴 교체와 무관함을 확인하고 이미지 패치로 처리했습니다.

## 미확인

- 스토리 전 챕터 완주, 요괴 대도감·게시판·신문·상점 화면의 모든 페이지, 엔딩.
- 실제 PS2 기기와 다른 에뮬레이터.
- `D_EFF.BIN` 아이템 효과 라벨과 RenderWare 텍스처는 미적용이므로 일본어로 표시됩니다.

## 확인 방법 (PCSX2 기본 키)

Enter = START, L = ○(결정), K = ✕(취소), 방향키 = 이동, F8 = 스크린샷. 원본 ISO로 만든 상태 저장은 VRAM에 옛 글꼴이 남을 수 있으니 새로 부팅해서 확인하세요.
