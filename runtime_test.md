# 실행 확인 기록

에뮬레이터 확인은 사용자가 직접 수행하며, 빌드 스크립트와 검수 도구는 에뮬레이터를 실행하지 않습니다. 아래는 이 저장소를 공개하는 시점에 기록으로 남아 있는 범위입니다. 합성 미리보기(`--preview` 출력, `mess_render.py` 렌더링)는 게임 실행 화면으로 간주하지 않습니다.

## v1.0.0 빌드 (2026-09-10, 결과 ISO SHA-1 `396bbb0ccf92348477d07339ac36e67bcc7d2124`)

- 정독 검수 후 `D_MESS_ko.BIN`을 다시 주입하고 ISO·xdelta를 다시 만든 빌드입니다. 글꼴·이름표·UI 이미지·메뉴 배경 파일은 2026-09-07~09 빌드와 같습니다.
- 자동 검수(`qa_text.py`, `qa_img.py`)와 xdelta 왕복 검사는 이 빌드에서 통과했습니다.
- 이 빌드의 PCSX2 실행 확인은 사용자 몫으로 넘겼으며, 이 문서를 쓰는 시점에 확인 결과가 기록되어 있지 않습니다. 대사가 많이 바뀐 곳은 무지나 편(서브파일 0x78800), 큰지네 동굴(0x7c000), 기가 성 최종전(0x9a800), 오소레잔(0xb7800)입니다.

## 이전 빌드에서 PCSX2 2.2.0으로 확인한 것

- 2026-09-06: 한글 글꼴을 넣은 `D_MOJI.BIN`으로 부팅 → 타이틀 → 시작 → 인트로 내레이션의 한자 자리에 한글이 코드 계산값대로 표시됨. 프로브 빌드(모든 메시지 첫 두 글자를 코드 0x0B2D·0x0A00으로 교체)에서 빈 글리프 블록 12·13의 글자(힝·통)가 인트로 첫 줄에 정확히 표시되어 2,350자 전부 사용 가능함을 확인.
- 2026-09-07: 사용자의 플레이 스크린샷으로 초반 대사와 대사창 이름표를 확인. 이름표는 폰트가 아니라 `D_CHFONT.BIN`의 이미지라는 사실을 여기서 확정하고 한글 이름표를 만들었습니다. 이 단계에서 아직 번역되지 않은 메시지는 한자 자리에 무작위 한글이 섞여 보였는데, 이는 글꼴 오류가 아니라 미번역 메시지의 표시 특성입니다(현재는 전부 번역됨).
- 메인 메뉴의 글자(始めから 등)와 챕터 타이틀·상태 이상 등은 미리 렌더링된 이미지라 글꼴 교체와 무관함을 확인하고 이미지 패치로 처리했습니다.

## 미확인

- 스토리 전 챕터 완주, 요괴 대도감·게시판·신문·상점 화면의 모든 페이지, 엔딩.
- 실제 PS2 기기와 다른 에뮬레이터.
- `D_EFF.BIN` 아이템 효과 라벨과 RenderWare 텍스처는 미적용이므로 일본어로 표시됩니다.

## 확인 방법 (PCSX2 기본 키)

Enter = START, L = ○(결정), K = ✕(취소), 방향키 = 이동, F8 = 스크린샷. 원본 ISO로 만든 상태 저장은 VRAM에 옛 글꼴이 남을 수 있으니 새로 부팅해서 확인하세요.
