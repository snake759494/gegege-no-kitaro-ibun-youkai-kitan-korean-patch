# 제작 및 구조

원본 파일 길이와 디스크 배치를 유지하는 인플레이스 패치입니다. ISO 안의 데이터 파일 4개를 같은 크기로 다시 만들어 원래 LBA 위치에 덮어쓰며, 실행 파일 `SLPM_653.37`은 손대지 않습니다. 아래 형식은 전부 이 프로젝트에서 직접 해석해 인게임 표시로 확인한 것입니다.

## 대사 글꼴 `D_MOJI.BIN`

- 헤더 없음, 글리프 3,584개, 24×26 px, 2bpp, 글리프당 156바이트(행당 6바이트). 559,104바이트.
- 바이트 안 픽셀 순서가 단순 MSB 순이 아닙니다: 픽셀0 = 비트 5~4, 픽셀1 = 비트 7~6, 픽셀2 = 비트 1~0, 픽셀3 = 비트 3~2. 단순 MSB 순으로 읽으면 모든 세로획이 이중선처럼 보이고, 그 순서로 주입하면 인게임 글자가 깨집니다.
- 픽셀 값(ELF 0x280820의 팔레트): 0 = 투명, 1 = 흰색, 2 = 회색 200, 3 = 회색 100(모두 불투명). 원본 글리프는 이것을 4단계 안티에일리어싱으로 씁니다.
- 게임은 글리프를 가로 약 0.8배, 세로 약 1.07배로 양선형 필터링해 그립니다. 원본 한자는 22×22 px(1~22열, 3~24행)를 차지합니다.
- 블록(256글리프): 0~2 ASCII(동일 복제), 3 가나(0x3AF~0x3B9는 "MEMORY CARD差込口" 같은 2글자 합자), 4~11 한자, 12~13 빈 채움.

## 텍스트 인코딩 `D_MESS.BIN`

- 서브파일 62개가 0x800 경계로 정렬되어 이어져 있습니다. 각 서브파일 = u16 자기상대 오프셋 테이블(엔트리 i는 2i + 값) + 0x1F로 끝나는 메시지들 + 0 패딩. 메시지 총 57,120개 슬롯 중 내용이 있는 것은 13,481개입니다.
- 1바이트 0x20~0x7F: ASCII 글리프(블록 0). 2바이트 [선행 0x00~0x0B][하위]: **글리프 = 코드 + 0x200** (0x01xx = 가나, 0x02xx~0x09xx = 한자). `02 00`은 전각 공백(글리프 0x400).
- 제어 코드: **0x1D 줄바꿈, 0x1E 대사창 넘김(다음 창, 입력 대기), 0x1F 메시지 끝.** 0x80 이상은 파라미터를 동반하는 제어·기호 코드: 〝=0x80, 、=0x85, 。=0x86, ・=0x87, 「=0x89, 」=0x8A, 0x8B/0x8C는 강조 표시의 시작/끝, 0x18은 디버그 메뉴 전용. …는 코드 0x1AC.
- 서브파일 지도: 0x00000 전투·필드 명령과 도움말, 0x01000 유닛 이름 목록, 0x03800 미사용 메카 목록, 0x04000 디버그 메뉴, 0x05000 메뉴 라벨, 0x06000 임무 목표, 0x07000 메모리 카드·세이브/로드 메시지, 0x09000 요괴 대도감(512~688)·모노노케WEB 게시판(0~511)·요괴타임스 기사·편지 애니메이션(800~818), 0x16000~0xc5000 스토리 대사 52개 서브파일.

## 한글 배치

- KS X 1001 완성형 2,350자를 순서대로 글리프 0x0400~0x0D2D에 기록합니다. 텍스트 코드로는 **0x0200(가)~0x0B2D(힝)**, 즉 코드 = 0x200 + KS X 1001 색인. 예: "가" = `02 00`, "힝" = `0B 2D`.
- 글리프 0x0400~0x0BFF(2,048자)는 원래 한자 영역, 0x0C00~0x0D2D(302자)는 빈 채움 블록 12·13입니다. 빈 블록도 게임이 정상 참조함을 프로브 빌드로 확인했습니다.
- 렌더링(`tools/hangul_font.py`): 서울한강체 Bold, 8배 슈퍼샘플링. 2,350자 전체 잉크 영역의 합집합을 원본 한자와 같은 22×22 px에 맞추고, 획 중심이 값 1(흰색)이 되도록 커버리지 임계값 0.62/0.38/0.14로 4단계 양자화합니다.
- 한자 글리프를 한글로 바꿨으므로, 번역되지 않은 일본어 메시지가 남아 있으면 그 자리에는 무작위 한글이 표시됩니다. 현재 배포본은 모든 메시지를 번역했지만, 제보에서 "깨진 한글"이 보이면 글꼴이 아니라 미번역 메시지를 의심합니다.

## 대사창 레이아웃 규칙 (실측)

- 한 줄 최대 폭 = 36 half-unit(전각 = 2, 반각 ASCII·공백 = 1). 원본 실측 최댓값이 36이며 패치는 여유를 두어 **34**에서 줄바꿈합니다.
- 대사창 최대 **3줄**. 넘치면 0x1E로 다음 대사창으로 분할합니다. 줄바꿈은 공백 기준 단어 단위이고, 한 단어가 한 줄보다 길면 글자 단위로 강제 분할합니다. 줄 앞의 공백은 들여쓰기로 보존합니다.
- 영역별 규칙(`tools/ko_inject.py`의 LAYOUT): 요괴 대도감 페이지(0x09000 512~688) 20 half-unit × 21줄, 게시판(0x09000 0~511) 34 × 37, 대도감 이름·편지(0x09000 689~) 34 × 21, 임무 목표(0x06000) 28 × 3, 세이브·아이템 패널(0x07000) 36 × 6, 디버그 메뉴(0x04000) 34 × 99(`|`가 줄바꿈이 아니라 목록 구분자라 페이지를 나누지 않음).
- 원본이 0x1E를 한 번도 쓰지 않는 서브파일은 게임이 페이지를 넘기지 못한다고 보고, 그런 곳에서 한국어가 2페이지가 되면 자동 검수가 잡아냅니다.

## 캐릭터 이름표 `D_CHFONT.BIN`

64개 서브블록 × 0x2000바이트. 각 서브블록은 0x48의 팔레트(u16 RGBA5551, 0 = 투명, 1 = 흰색 중심, 2 = 중간 회색, 3 = 어두운 외곽)와 0x68부터 96×32 4bpp 이미지 4장(각 0x600바이트, 하위 니블 = 왼쪽 픽셀)입니다. 총 138장이 실제 이름표이며 대사 글꼴과 같은 임계값으로 한글을 렌더링했습니다. 대사창의 이름은 서브파일 0x01000의 텍스트가 아니라 이 이미지로 표시됩니다.

## 스프라이트 컨테이너 `D_SYS.BIN` / `D_BG.BIN`

```
u16 magic (0x5f51 | 0x5f71)
u16 count
u32 dataStart
u32 pad (= 0)
count × { u32 A, u16 w, u16 h, u16 w/2, u16 h/2, u32 nextOff }   // +8부터
u16 palette[256] (8bpp) | palette[16] (4bpp)   // dataStart 바로 앞, RGBA5551
8bpp/4bpp 선형 픽셀 (dataStart부터 이어붙임)
```

- **레코드의 `nextOff`는 자기 이미지가 아니라 "다음" 이미지의 오프셋**입니다. i번 이미지 위치 = (i == 0 ? dataStart : record[i-1].nextOff). 마지막 레코드의 값은 더미(0x100). 이걸 틀리면 5장 중 1장꼴로 대각선 잡음처럼 디코딩되는데, PS2 스위즐이 아닙니다.
- 판별식: `dataStart == 8 + count*16 + 512`(8bpp) 또는 `+ 32`(4bpp), `pad == 0`. 0x800 정렬 오프셋을 훑어 찾습니다.
- 4bpp는 하위 니블이 먼저(픽셀 짝수 = `b & 15`, 홀수 = `b >> 4`).
- 팔레트 bit15는 전부 0이라 알파로 쓰이지 않습니다. **인덱스 0이 크로마키(투명)**이며 팩마다 색이 다릅니다(초록 0x0260 / 파랑 0x7c00 / 마젠타 등). RGB = `((v & 31), (v >> 5 & 31), (v >> 10 & 31)) * 255 // 31`.
- `D_SYS.BIN` 주요 팩: 0x040800 전투·메뉴 UI 83장, 0x096800 웹 로고, 0x0a6000 휴대폰 안내문(4bpp), 0x0bf800 상점 정렬, 0x0d0000 상점 안내문(4bpp), 0x0d6000 웹 링크 버튼, 0x0eb000/0x0f7800 세이브 슬롯 패널, 0x147800 세로 장소 간판, 0x484800~0x51a800 챕터 타이틀 44장(팩당 4bpp 1장), 0x587800~ 이벤트 썸네일.
- `D_BG.BIN`은 같은 컨테이너로 전체 화면 8bpp 이미지 176장을 담습니다. #66이 게임 로고, #68~#72·#77~#79·#87이 라벨이 그려진 휴대폰·상점·신문 메뉴 화면입니다.

## 한글 이미지 렌더러 `tools/koimg.py`

TTF를 4배 슈퍼샘플링해 외곽선·본체 마스크를 만들고 합성한 뒤 팩 자체 팔레트로 최근접 양자화합니다. 배경을 살리는 판(plate) 모드: `make_plate`(같은 크기 스프라이트 N장 중 가장 어두운 픽셀 + 인페인트, 돌판 태그 5장), `fill_inside`/`flat_plate`(평면 패널, `margin=`으로 지운 영역 바깥을 샘플링), `region_plate`의 lerp/row/box/tile(tile은 주기 14 px의 점선 띠를 재구성, 신문 헤더), `inpaint_ink`(색 조건에 맞는 픽셀만 제거하고 팽창 약 5로 어두운 외곽선까지 지움, 질감 있는 배경용). 파일 크기는 바뀌지 않습니다.

## 이미지로 그려진 텍스트와 처리 상태

| 파일 | 내용 | 상태 |
| --- | --- | --- |
| `D_SYS.BIN` 0x040800 | 전투/메뉴 UI(반격 명령·작전 목적·저장·불러오기·설정·상태 이상·결착·로딩 중·아이콘) | 한글화 |
| `D_SYS.BIN` 0x096800 / 0x0a6000 / 0x0d6000 | 휴대폰 웹 로고·안내문·링크 버튼 | 한글화(요괴타임스 눈알 로고 제외) |
| `D_SYS.BIN` 0x0bf800 / 0x0d0000 | 상점 정렬 버튼·안내문 | 한글화 |
| `D_SYS.BIN` 0x147800 | 세로 장소 간판 4장 | 한글화 |
| `D_SYS.BIN` 0x484800~0x51a800 | 챕터 타이틀 44장(4bpp) | 한글화 |
| `D_BG.BIN` #68~#87 | 휴대폰/상점/신문 메뉴 배경 9장 | v1.0.0에서 한글화했으나 프롤로그 멈춤의 원인으로 특정되어 v1.0.1부터 원본 유지 (`tools/patch_bg.py`는 참고용) |
| `D_BG.BIN` #66 | 게임 로고(ゲゲゲの鬼太郎 異聞妖怪奇譚) | 일러스트라 보존 |
| `D_CHFONT.BIN` | 캐릭터 이름 플레이트 138장 | 한글화 |
| `D_EFF.BIN` | 아이템 효과 라벨 11장(体力回復 등) | 미적용 — 0x5f51 컨테이너가 아니라 별도 해석 필요 |
| `D_TXD.BIN`, `D_BTTF~1.BIN` | RenderWare 텍스처(맵 재질, gd02 128×128 8bpp 스위즐) | 미적용 — PS2 GS 언스위즐 디코더 필요 |

## ISO 패치 `tools/build_iso.py`

pycdlib로 원본 ISO의 파일 레코드에서 LBA와 길이를 읽고, 같은 크기의 교체 파일을 출력 ISO의 그 위치에 덮어쓴 뒤 다시 읽어 검증합니다. UDF/ISO9660 구조를 다시 쓰지 않으므로 나머지 바이트는 원본과 완전히 같고, xdelta 패치 크기도 실제 바뀐 데이터 분량(약 650 KB)에 그칩니다.
