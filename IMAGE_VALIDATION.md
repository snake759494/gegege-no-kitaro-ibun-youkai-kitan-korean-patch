# UI 이미지 재적용 검증 규칙

2026-09-16 v1.0.3 검증 기록입니다. 사용자가 재적용 후보의 정상 진행을 확인하고 릴리즈를 요청했습니다. 아래 자동 시험의 미완료 범위는 사용자 진행 확인과 구분하여 유지합니다.

## 합격 조건

정적 검사만 통과한 ISO를 “프리징 해결판”으로 판정하지 않습니다. 다음 조건을 각각 기록합니다.

1. **원본 고정:** D_SYS·D_BG의 SHA-256이 `tools/image_safety.py`에 고정한 일본판 원본과 일치해야 합니다. 이전 패치 결과를 다시 입력으로 쓰지 않습니다.
2. **구조 보존:** 파일 길이, 헤더, 레코드, 팔레트, 패딩과 미지정 이미지의 모든 비트를 보존합니다. 이미지 크기와 오프셋이 파일 및 다음 컨테이너 경계를 넘으면 중단합니다.
3. **수정 범위 명시:** D_SYS는 지정한 99개 스프라이트만, D_BG는 9장 안에 지정한 사각형만 수정합니다. 변경 구간이 허용 영역과 일부 겹친다는 이유로 통과시키지 않고, 변경한 모든 비트를 검사합니다. 요청한 이미지가 하나라도 미변경이면 실패입니다.
4. **재현성:** 독립 실행으로 다시 생성한 BIN의 해시가 일치해야 합니다. 픽셀 쓰기는 유효한 팔레트 인덱스만 허용합니다. 4비트 이미지의 미사용 마지막 반바이트는 보존합니다.
5. **ISO 격리:** 출력 ISO는 새 파일이어야 합니다. 같은 크기의 기존 ISO도 재사용하지 않습니다. 원본과 전체 비교해 선언한 교체 파일 밖의 변경이 0바이트이고 각 파일 내용이 검증한 BIN과 일치해야 합니다.
6. **실행 비교:** 같은 PCSX2 버전·설정·메모리 카드·입력 순서로 UI 원본 / SYS만 / BG만 / SYS+BG를 비교합니다. 냉부팅 또는 메모리 카드 로드를 사용합니다. 다른 빌드의 상태 저장을 읽는 것만으로 이미지 로드를 검증하지 않습니다.
7. **프리징 구분:** 화면 변화나 FPS만으로 진행을 판정하지 않습니다. 입력이 실제 컨트롤러에 전달되는지, 대사 넘김·시점·위치·이벤트 전환이 반응하는지 함께 확인합니다. 자동 ○ 입력은 대사 진행에만 제한하고 탐색 전에는 해제합니다. 실행 코드·스크립트·완료 플래그를 수정한 시험은 통과 근거로 쓰지 않습니다.
8. **결과 범위:** 비교하지 못한 챕터·이미지·실기는 미검증으로 기록합니다. 차이가 재현되면 `patch_sys.py --groups` / `patch_bg.py --images`로 분할해 동일 조건으로 재시험합니다. 모든 경우에서 정지하면 패치 인과관계가 입증되지 않은 것이며 입력·원본 게임 조건도 조사합니다.

## 수정한 도구 결함

- `build_iso.py`: 기존 출력 재사용으로 제거한 패치가 남을 수 있던 동작을 제거했습니다. 입력과 같은 경로 및 기존 출력은 작업 전에 거절합니다.
- `koimg.py`: 전역 색상 캐시가 수명이 끝난 팔레트의 객체 ID를 키로 사용했습니다. ID 재사용 시 다른 팔레트의 인덱스가 섞일 수 있어 팔레트 내용 자체를 키로 바꿨습니다.
- `sysimg.py`: 팔레트 길이만으로 픽셀 비트 수를 판단했습니다. `0x5f71`이면서 256색 팔레트를 가진 D_SYS의 두 리소스를 8비트로 오독했으며, 특히 `0x92000`은 다음 컨테이너 `0x96800`까지 겹쳐 읽었습니다. 포맷 값에 따라 4비트로 해석하면 모든 70개 SYS / 176개 BG 컨테이너가 서로 겹치지 않습니다. 전체 픽셀 읽기→쓰기 왕복으로 메타데이터 보존을 확인합니다.
- `qa_img.py`: 누락된 결과 파일을 건너뛰고, 실패해도 종료 코드 0을 반환했으며, 변경 구간이 아무 이미지와 일부 겹쳐도 허용했습니다. 필수 파일 누락·금지 비트 변경·의도하지 않은 이미지 수정은 이제 비정상 종료합니다.

이는 재현 가능한 제작·검수 도구의 결함입니다. 각각이 제보된 게임 프리징의 직접 원인이라는 주장은 하지 않습니다. 특히 이번 SYS 재생성 결과는 기존 SYS 한글 BIN과 동일합니다.

## 로컬 후보 생성

기존 글꼴·대사·이름표는 v1.0.2와 같습니다. 아래 출력 폴더는 먼저 만들고 ISO 출력은 존재하지 않는 경로를 사용합니다.

```powershell
python tools/patch_sys.py --output validation_20260916/D_SYS_candidate.BIN
python tools/patch_bg.py --output validation_20260916/D_BG_candidate.BIN
python tools/qa_img.py --sys validation_20260916/D_SYS_candidate.BIN --bg validation_20260916/D_BG_candidate.BIN --report validation_20260916/static_audit.json
python tools/test_image_safety.py
python tools/build_iso.py "Gegege no Kitarou Ibun Youkai Kitan.iso" Kitaro_KR_UI_candidate_20260916.iso D_MOJI.BIN=D_MOJI_hangul2350.BIN D_MESS.BIN=D_MESS_ko.BIN D_CHFONT.BIN=D_CHFONT_ko.BIN D_SYS.BIN=validation_20260916/D_SYS_candidate.BIN D_BG.BIN=validation_20260916/D_BG_candidate.BIN
python tools/verify_iso_scope.py "Gegege no Kitarou Ibun Youkai Kitan.iso" Kitaro_KR_UI_candidate_20260916.iso D_MOJI.BIN=D_MOJI_hangul2350.BIN D_MESS.BIN=D_MESS_ko.BIN D_CHFONT.BIN=D_CHFONT_ko.BIN D_SYS.BIN=validation_20260916/D_SYS_candidate.BIN D_BG.BIN=validation_20260916/D_BG_candidate.BIN --report validation_20260916/iso_audit.json
```

## 정적 검사 결과

- SYS: 99장, 429,349바이트 변경. BG: 9장, 179,786바이트 변경. 허용 영역 밖 변경 0.
- SRC/SYS/BG 컨테이너 경계 검사, 메타데이터 보존, 미지정 이미지/사각형 밖 변경 거부, 팔레트 ID 충돌 회귀, ISO 재사용 거부 등 11개 회귀 검사 통과.
- BG 독립 재생성 결과 동일: `9ed9867866dc3eabf40c653729e8a74b175f4e08f0622514d7f6254ea8e495fa`.
- 후보 ISO SHA-256: `b673e0832b9bd4e23ddf6527b836e8c90bab81e04b05502900bd4b7e6a9a9ca3`.
- ISO 전체 비교: 선언한 5개 파일 밖 변경 0.

## 실행 검사 현황 — 미완료

- 사용자가 기존 프리징 당시 방향·이동 입력 모두 무반응이었다고 확인했습니다. 단순 대사 대기로 단정하지 않습니다.
- PCSX2 v2.9.52의 별도 데이터 폴더와 복사한 메모리 카드로 시험했습니다. 사용자 원래 상태 저장과 메모리 카드는 시험 출력으로 덮어쓰지 않았습니다.
- UI 원본 기준판 `Kitaro_DIAG_SYS.iso`는 냉부팅 후 새 게임부터 병원 탐색까지 진행했습니다. 에뮬레이터 컨트롤러 매크로를 통해 이동·회전 반응을 확인했습니다. 이 결과는 제보된 문 진입 이후 구간의 통과를 뜻하지 않습니다.
- 프레임별 입력 재생 파일을 만들었으나, 재생 파일 교체 시 시작 프레임이 이전 실행에서 이어지는 현상이 있어 해당 경로 비교 결과는 합격 근거에서 제외했습니다.
- 에뮬레이터 프로세스를 새로 시작하면 재생이 0프레임부터 시작했습니다. 복도 이동·회전과 다음 대사 이벤트를 확인했지만, 자동 경로는 문 진입까지 완성하지 못했습니다.
- 보존한 실제 멈춤 상태 `freeze_before.p2s`를 당시와 같은 `Kitaro_DIAG_SYS.iso`에 불러온 별도 시험에서는 정상 컨트롤러 매크로로 방향과 이동 모두 반응했습니다. 방향 `(0,0,-1)` → `(-1,0,0)`, 위치 x `10.1592` → `-10.1600`으로 변했습니다. 게임 메모리에 입력을 직접 쓰거나 코드·이벤트 플래그를 수정하지 않았습니다. 전후 상태 저장의 실행 코드 `0x100000..0x2b0000` 및 시나리오 `0x39af94..0x3aaf94` 비교도 변경 0바이트입니다. 이 결과는 별도 실행 환경에서 저장 상태가 입력에 반응한다는 뜻이며, 당시 입력 무반응의 원인이나 후보판 전체 안정성을 입증하지는 않습니다.
- SYS만 / BG만 / SYS+BG 후보의 ISO 변경 범위 검사는 완료했습니다. 동일 경로의 냉부팅 실행 비교 및 문제의 문 진입 이후 검사는 아직 완료하지 못했습니다.
- SYS+BG 후보 `Kitaro_KR_UI_candidate_20260916.iso`를 상태 저장 없이 새로 부팅하여 새 게임 → 도입부 → 병원 탐색까지 진행했습니다. 정상 컨트롤러 입력으로 위치 z `-180.0` → `-109.9588`, 방향 `(0,0,1)` → `(-1,0,0)` 변화를 확인했습니다. 시험 후 자동 입력을 모두 해제하고 별도 에뮬레이터를 일시 정지했습니다. 원래 문제의 문 진입 이후 구간은 아직 검증하지 못했습니다.
- 이 후보의 부팅·도입부 디스크 로그에서는 수정한 SYS 이미지 26개의 픽셀을 포함한 섹터 읽기를 관측했고, 수정한 BG 9개의 픽셀 섹터 읽기는 관측하지 못했습니다. 섹터 읽기 관측은 해당 이미지 전체의 화면 표시 검증과 다릅니다. 나머지 SYS와 BG 메뉴는 실행 미검증입니다.

현재 후보는 **정적 검증 통과, 실행 검증 미완료**입니다. 도구 결함 수정만으로 제보된 프리징의 근본 원인을 해결했다고 판정하지 않습니다. 사용자의 정상 진행 확인 및 명시적 릴리즈 요청에 따라 이 후보와 동일한 결과를 v1.0.3으로 배포합니다. 사용자 확인이 아래 미검증 범위까지 포함한다는 뜻은 아닙니다.
